<!DOCTYPE HTML>
<html>
<body>


<script>
  var code = [":", ";", "~", "<", ">", "+","£", "€", "%", "#", "=", '"', "$", "^", "'", "/", '\\', "_", "|", "1", "3", "4", "2", "5", "7", "6"]
  function decrypt(element, index, array) {
    console.log(element);
        index=(element.charCodeAt(0)-97);
        console.log(code[index]);
        document.getElementById("encryption").value = (code[index])
  }

  function process() {
    var x = document.getElementById("encryption").value
    var xchr = x.split("");
    xchr.forEach(decrypt);

  }
</script>
  Code: <input type="text" id="encryption"></br>
  <button onclick="process()">Decrypt</button>
</body>
</html>
